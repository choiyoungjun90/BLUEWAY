import React from 'react';
import { Search, ShoppingCart, ArrowRight, Menu } from 'lucide-react';

// 각 탭에 해당하는 컴포넌트들
/*
const renderContent = () => {
  switch (activeTab) {
    case 'about':
      return <About />;
    case 'products':
      return <Products />;
    case 'contact':
      return <Contact />;
    default:
      return <About />;
  }
};
*/

// 네비게이션 바 컴포넌트
const Navbar = () => {
  return (
    <nav className="absolute top-0 left-0 w-full z-20 px-8 py-6 flex justify-between items-center text-white">
      {/* 로고 영역 */}
      <div className="flex items-center gap-2">
        <span className="text-lime-400 text-2xl">🌱</span> {/* 로고 아이콘 대체 */}
        <h1 className="text-2xl font-bold tracking-wide">AgroLink</h1>
      </div>

      {/* 메뉴 링크 (데스크탑) */}
      <div className="hidden md:flex gap-8 text-sm font-medium tracking-wide text-gray-200">
        <a href="#" className="hover:text-white transition">Home</a>
        <a href="#" className="hover:text-white transition">About</a>
        <a href="#" className="hover:text-white transition">Products</a>
        <a href="#" className="hover:text-white transition">Contact Us</a>
      </div>

      {/* 우측 아이콘 */}
      <div className="flex items-center gap-6">
        <button className="hover:text-lime-400 transition">
          <Search size={20} />
        </button>
        <button className="hover:text-lime-400 transition">
          <ShoppingCart size={20} />
        </button>
        <button className="md:hidden">
          <Menu size={24} />
        </button>
      </div>
    </nav>
  );
};

// 메인 히어로 섹션 컴포넌트
const HeroSection = () => {
  return (
    <section className="relative h-[85vh] w-full bg-gray-900 flex items-center overflow-hidden">
      {/* 배경 이미지 */}
      <div className="absolute inset-0 z-0">
        <img 
          src="src/assets/background.jpg" 
          alt="background"
          className="w-full h-full object-cover opacity-60"
        />
        {/* 어두운 오버레이 (텍스트 가독성용) */}
        <div className="absolute inset-0 bg-black/40"></div>
      </div>

      {/* 텍스트 콘텐츠 */}
      <div className="relative z-10 container mx-auto px-8 md:px-16">
        <div className="max-w-2xl">
          <h2 className="text-5xl md:text-6xl font-bold text-white leading-tight mb-6">
           BLEU WAY
          </h2>
          <p className="text-gray-300 text-lg md:text-xl font-light mb-8 max-w-lg">
            We are planning to grow as a global corporation that maximize customer satisfaction with best prices, highest quality, on-time delivery, and strong supply chain establishment. 
          </p>
        </div>
      </div>
    </section>
  );
};

// 하단 'We're Global' 섹션 컴포넌트
const StatsSection = () => {
  return (
    <section className="bg-white py-16 px-8 md:px-16">
      <div className="container mx-auto">
        <div className="flex flex-col md:flex-row justify-between items-start md:items-center mb-12">
          
          {/* 왼쪽 텍스트 및 링크 */}
          <div className="mb-8 md:mb-0">
            <a href="#" className="flex items-center gap-2 text-black font-semibold mb-4 hover:gap-3 transition-all">
              See all Produce <ArrowRight size={18} className="text-lime-600" />
            </a>
            <h3 className="text-4xl font-bold text-gray-800">
              We're <span className="text-lime-600">Global</span>
            </h3>
          </div>

          {/* 여기에 지도나 그래프 이미지가 들어갑니다 */}
          <div className="flex-1 w-full md:ml-20 flex gap-4 md:gap-8 overflow-hidden items-end h-32">
             {/* 예시용 그래프 플레이스홀더 */}
             <div className="bg-yellow-400 w-8 h-20 rounded-t-md relative group">
                <span className="absolute -top-6 text-xs font-bold">100%</span>
             </div>
             <div className="bg-lime-500 w-8 h-12 rounded-t-md"></div>
             
             {/* 지도 이미지 플레이스홀더 */}
             <div className="flex-1 bg-blue-50 rounded-lg h-full flex items-center justify-center text-blue-300">
                [World Map Image]
             </div>

             {/* 원형 차트 플레이스홀더 */}
             <div className="w-20 h-20 rounded-full border-4 border-lime-500 border-t-yellow-400"></div>
          </div>
        </div>
      </div>
    </section>
  );
};

// 메인 앱 컴포넌트
export default function AgroLinkPage() {
  return (
    <div className="font-sans">
      <Navbar />
      <HeroSection />
      <StatsSection />
    </div>
  );
}